org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError

android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template, and cannot be
# flipped to true yet. Turning it on discards the KGP declared in
# settings.gradle and falls back to the one AGP bundles — which is 2.2.10 in
# every AGP from 9.1.0 to 9.3.2, below the 2.2.20 that Flutter's
# DependencyVersionChecker treats as a hard error.
android.builtInKotlin=false
